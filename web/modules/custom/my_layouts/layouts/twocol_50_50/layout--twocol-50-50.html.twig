{#
/**
 * @file
 * Default theme implementation to display a two-column 50-50 layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 * @see https://www.drupal.org/docs/8/api/layout-api/how-to-register-layouts
 */
#}
<div{{ attributes.addClass(['layout', 'layout--twocol']) }}>
  {% if content.first %}
  <div{{ region_attributes.first.addClass(['col']) }}>
    {{ content.first }}
  </div>
  {% endif %}
  {% if content.second %}
  <div{{ region_attributes.second.addClass(['col']) }}>
    {{ content.second }}
  </div>
  {% endif %}
</div>